# You'll be pushing to a PUBLIC repo — this file exists specifically so your
# API key never ends up there by accident. Do not remove the .env line below.
.env
.env.local
.env.*.local
node_modules/
.DS_Store

# --- Trata (Next.js app at the repo root) ---
.next/
out/
next-env.d.ts
tsconfig.tsbuildinfo
# Regenerated by `next dev` on every start (next.config `agentRules`)
AGENTS.md
CLAUDE.md
# Internal design-system review artefacts. This remote is public.
*.zip
